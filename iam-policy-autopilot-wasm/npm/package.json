{
  "name": "iam-policy-autopilot-wasm",
  "version": "0.2.1",
  "description": "Browser-ready WebAssembly build of IAM Policy Autopilot — generates least-privilege IAM policies from source code. Requires Chrome 131+ (JSPI). Not yet optimized for production use (large binary).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "browserslist": [
    "chrome >= 131"
  ],
  "keywords": [
    "iam",
    "aws",
    "policy",
    "wasm",
    "webassembly"
  ],
  "license": "Apache-2.0",
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}